# Front-fast full-flow review

**Verdict:** The current 41-stage candidate completed its planned nominal Isaac simulation, from P0 through `FINAL_HOLD`. It does **not** meet the requested “normal and correct full climb onto the load-bearing backside” acceptance: both rear feet land on the shared edge between the 12-degree broad slope and the 25-degree outer bevel. Edge clearance to the intended broad support patch is zero. This is a terminal geometry issue, not a failed stage transition or a failed simulator gate.

This review is read-only for config and runtime. Current candidate SHA-256: `d15bf4d9f9a8feca09ebb1290502176e539f5b56c8b751ce9f9d0161f6601898`.

## P1 finding: rear terminal feet are at the support-patch edge

`AH_BACK_LOW_FIRST_SEGMENT` ends LB/RB at planned world x=.19 m and z=.150751 m; the `FINAL_HOLD` target preserves those anchors. See [climb_front_fast.json:9609](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/config/climb_front_fast.json:9609) and [climb_front_fast.json:9870](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/config/climb_front_fast.json:9870).

At the C41 `FINAL_HOLD` entry, measured simulator foot centers are LB `(.175292,.027767,.147629) m` and RB `(.176120,-.165888,.147927) m`. Their nearest points are both `(.177920890, y, .141683638) m`, exactly where the broad 12-degree slope and 25-degree bevel meet; the nearest visual triangles are 16198 and 17143. The point is on the broad patch boundary, not inside it. The signed sphere gaps near zero in [final_actual_foot_stl_audit.json](/home/artrc/Grasp_hexapod/src/docs/evidence/climb_front_fast_20260923/final_actual_foot_stl_audit.json) mean geometric proximity only; they do not mean the foot reached the intended patch.

A provisional smaller change is to move the AH rear targets by +20 mm in world x, to about `.21 m`, and recompute the STL height, active-base knots, terminal joint solution, and AH-to-hold continuity. The main-thread endpoint DLS probe found `.205/.21 m` reachable; at `.21 m`, B z is `.15500226 m`, residual `8.31e-7 m`, joint margin `.267816 rad`, and terminal capsule guard clear. This is an endpoint probe only. The modified route and landing have not been continuously checked or simulated; do not treat this as an accepted repair.

## Structural and execution checks

- The config contains 41 stages, with the nine recorded merges. K rear world-x ends at `.00 m` (from `.03 m`); N ends at `.01 m` (from `.05 m`) and reset entry is `.01 m`. The added `BODY_B_FRONT_INWARD` merge retains active legs `[1,4]`, 20 mm apex, and the source full-stage pose path. See the stage map/config around [BODY_B_FRONT_INWARD](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/config/climb_front_fast.json:827) and back-spacing metadata [lines 10018–10023](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/config/climb_front_fast.json:10018).
- A lightweight mathematical check evaluated `_stage_reference()` at all 40 stage-end / next-stage-start pairs: maximum pose discontinuity 0; maximum anchor discontinuity `5.6e-17 m`. Non-active leg world anchors remain unchanged at every listed knot. AH to `FINAL_HOLD` is continuous. The plan has all six foot anchors above the initial floor at terminal hold; this says where the model plans to put them, not whether each has an adequate support patch.
- Runtime config validation and command-cap compatibility are checked in [run_sim.py:1124–1142](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/scripts/run_sim.py:1124). Front simulation enters with `hardware_execution=False`; front hardware execution is rejected by [climb_mode.py:436–437](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/scripts/climb_mode.py:436). The 6 rad/s controller cap is simulation-only and is below the 7.2 rad/s Isaac DOF cap for `--climb-joint-speed 1.8`; [control.py:1157–1176](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/scripts/control.py:1157) retains the shared 4 rad/s cap on other paths.
- Simulation-only `preview_time_only_stage_advance` advances by planned phase and settle time, not by contact or load evidence; see [climb_mode.py:982–1005](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/scripts/climb_mode.py:982). This behavior matches the candidate’s simulation-only scope. Full-mission and ROS front execution are rejected in [run_sim.py:1118–1122](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/scripts/run_sim.py:1118).
- GPU config and runtime source hashes match the pre-start invocation record. Metrics cover all 41 config stages C1–C41. The run ended `DONE`, reason `none`, after `43.6667 s`; clips `0`; all five console preview gates passed. Max target speed was `5.0312 rad/s`; max foot error `11.732 mm`; max root error `21.287 mm`; max support drift `9.239 mm`; minimum joint margin `0.011683 rad`. The four extra threshold checks also passed. See [gpu_summary.json](/home/artrc/Grasp_hexapod/src/docs/evidence/climb_front_fast_20260923/gpu_summary.json) and [gpu_console.log:145–156](/home/artrc/Grasp_hexapod/src/docs/evidence/climb_front_fast_20260923/gpu_console.log:145).

## Evidence boundaries

The selected Q capsule proxy warning has a local mesh receipt: four body-to-LM/RM knee/ankle pairs at the recorded witness and the worst-gap body-to-LM-knee sample at 1.2667 s were triangle-clear by conservative AABB/broadphase exclusion. The rear-knee pair checks also cover only their specified sampled poses. Neither result proves containment or continuous clearance.

The robot actor is created with collision-filter argument `1` in [run_sim.py:1266](/home/artrc/Grasp_hexapod/src/grasp_hexapod_control/scripts/run_sim.py:1266), so the GPU run does not establish full-robot self-collision freedom. No simulation result proves real contact, load paths, friction, slip resistance, stability, actuator performance, or hardware readiness.

Focused test history is recorded accurately in the evidence README: the initial run was 10 passed / 2 assertion failures; after fixing the assertions, the two affected tests passed in a targeted rerun. No full regression or simulation was repeated for this review.
